def plusMinus(arr):
    print(len([x for x in arr if x > 0])/n)
    print(len([x for x in arr if x < 0])/n)
    print(len([x for x in arr if x == 0])/n)


n = int(input())

arr = list(map(int, input().rstrip().split()))

plusMinus(arr)
